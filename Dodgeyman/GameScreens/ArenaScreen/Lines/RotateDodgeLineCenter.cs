namespace Dodgeyman.GameScreens.ArenaScreen.Lines
{
    /// <summary>
    /// Where the center of rotation is for a <see cref="RotateDodgeLine"/>. That is, where
    /// the point that doesn't move is located.
    /// </summary>
    enum RotateDodgeLineCenter
    {
        BottomLeft,
        BottomRight,
        TopLeft,
        TopRight,
    }
}
